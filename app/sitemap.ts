import type { MetadataRoute } from "next";
import { expertises, sectors } from "../lib/site-data";
export default function sitemap(): MetadataRoute.Sitemap { const base = "https://geef-senegal.vercel.app"; const staticRoutes = ["", "/cabinet", "/expertises", "/secteurs", "/realisations", "/publications", "/equipe", "/formations", "/carrieres", "/contact", "/soumettre-un-projet", "/brochure", "/mentions-legales", "/confidentialite", "/cookies", "/conditions-utilisation", "/en", "/en/about", "/en/expertise", "/en/sectors", "/en/case-studies", "/en/insights", "/en/our-team", "/en/training", "/en/careers", "/en/contact", "/en/submit-a-project", "/en/brochure"]; return [...staticRoutes.map((route) => ({ url: `${base}${route}`, lastModified: new Date(), changeFrequency: route === "" ? "weekly" as const : "monthly" as const, priority: route === "" ? 1 : .7 })), ...expertises.map((item) => ({ url: `${base}/expertises/${item.slug}`, lastModified: new Date(), changeFrequency: "monthly" as const, priority: .75 })), ...sectors.map((item) => ({ url: `${base}/secteurs/${item.slug}`, lastModified: new Date(), changeFrequency: "monthly" as const, priority: .7 }))]; }
